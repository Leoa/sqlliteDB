<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.pace.pcd.sqllitedatabase" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".MyActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MySQLiteHelper"
            android:label="@string/title_activity_my_sqlite_helper" >
        </activity>
        <activity
            android:name=".Positions"
            android:label="@string/title_activity_positions" >
        </activity>
        <activity
            android:name=".PositionsDataSource"
            android:label="@string/title_activity_positions_data_source" >
        </activity>
    </application>

</manifest>
